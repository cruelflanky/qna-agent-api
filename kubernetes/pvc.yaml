apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: qna-agent-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  # storageClassName: standard  # Uncomment and set for your cluster
