apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: default

resources:
  - deployment.yaml
  - service.yaml
  - configmap.yaml
  - pvc.yaml
  # - ingress.yaml  # Uncomment if using ingress

# Image customization
images:
  - name: qna-agent
    newTag: latest
